#include <bits/stdc++.h>
using namespace std;

int main()
{
    int size_of_array;
    cin >> size_of_array;

    vector<int> values;

    for(int i = 0; i < size_of_array; i++)
    {
        values.push_back(i);
    }

    // precompute
    vector<int> hash(13, 0);
    for(int i = 0; i < 13; i++)
    {
        hash[values[i]]
    }

    int queries;
    cin >> queries;

    while(queries--)
    {
        int number;
        cin >> number;

        // fetch
    }
}
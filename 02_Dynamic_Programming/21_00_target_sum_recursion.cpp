#include <bits/stdc++.h> 
int helper(int index, int target, vector<int> &arr)
{
    if(index == 0)
    {
        /* check if the first element is totally divisible by target */
        if()
    }
}
int targetSum(int n, int target, vector<int>& arr) {
    
}

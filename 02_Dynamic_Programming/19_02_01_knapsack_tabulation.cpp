#include <bits/stdc++.h> 
int knapsack(vector<int> weightOfObject, vector<int> valueOfObject, int index, int maxWeight) 
{
	/* base case */
    for(int maximumWeight = weightOfObject[0]; )
}

/* tabulation i'll do it later on!, as of now i'm finding it difficult and tomorrow i need to submit this repo */
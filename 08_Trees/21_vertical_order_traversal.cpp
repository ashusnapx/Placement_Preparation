/* 
so in this code we'll use map and multiset.
so map<int, map<int, multiset<int>>>
so first int is for vertical (x - axis)
second int for level (y - axis)
third multiset<int> for node values and why multiset because a two
nodes can've same values
*/